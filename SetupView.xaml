<UserControl x:Class="BikeFitnessApp.SetupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1200"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             TextElement.FontWeight="Regular"
             TextElement.FontSize="14"
             TextOptions.TextFormattingMode="Ideal"
             TextOptions.TextRenderingMode="Auto"
             Background="{DynamicResource MaterialDesignPaper}"
             FontFamily="{materialDesign:MaterialDesignFont}">
    <Grid>
        <materialDesign:Card UniformCornerRadius="16" Margin="48" VerticalAlignment="Center" HorizontalAlignment="Center" Width="600" materialDesign:ElevationAssist.Elevation="Dp8">
            <Grid Margin="32">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="200"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,0,0,8">
                    <materialDesign:PackIcon Kind="Bike" Width="48" Height="48" HorizontalAlignment="Center" Foreground="{DynamicResource PrimaryHueMidBrush}"/>
                    <TextBlock Text="Smart Trainer Setup" Style="{DynamicResource MaterialDesignHeadline4TextBlock}" Margin="0,8,0,0" HorizontalAlignment="Center"/>
                </StackPanel>

                <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,24">
                    <ProgressBar Style="{DynamicResource MaterialDesignCircularProgressBar}" Value="0" IsIndeterminate="True" Width="20" Height="20" Margin="0,0,12,0" />
                    <TextBlock Text="Searching for your fitness machine..." Style="{DynamicResource MaterialDesignSubtitle1TextBlock}" Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                </StackPanel>
                    
                <ListBox x:Name="ListDevices" Grid.Row="2" Margin="0,0,0,24" 
                         Style="{DynamicResource MaterialDesignNavigationPrimaryListBox}"
                         DisplayMemberPath="Name"
                         ItemsSource="{Binding FoundDevices}"
                         SelectedItem="{Binding SelectedDevice}">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem" BasedOn="{StaticResource MaterialDesignNavigationPrimaryListBoxItem}">
                            <Setter Property="Padding" Value="16,12"/>
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>

                <StackPanel Grid.Row="3">
                    <TextBlock x:Name="TxtStatus" Text="{Binding Status, StringFormat={}Status: {0}}" Margin="0,0,0,16" HorizontalAlignment="Center" Style="{DynamicResource MaterialDesignCaptionTextBlock}"/>
                    <Button x:Name="BtnConnect" Content="CONNECT AND RIDE" Command="{Binding ConnectCommand}" 
                            Height="56" Width="300" FontSize="16" FontWeight="Bold" 
                            Style="{DynamicResource MaterialDesignRaisedButton}" 
                            materialDesign:ButtonAssist.CornerRadius="28"/>
                    <Button x:Name="BtnRescan" Content="RESCAN" Command="{Binding ScanCommand}" Margin="0,12,0,0" 
                            Style="{DynamicResource MaterialDesignFlatButton}" />
                </StackPanel>
            </Grid>
        </materialDesign:Card>
    </Grid>
</UserControl>