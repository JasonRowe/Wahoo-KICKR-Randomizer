<UserControl x:Class="BikeFitnessApp.SetupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1200" Background="White">
    <Grid Margin="48">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Wahoo KICKR Setup" FontSize="32" FontWeight="Bold" Margin="0,0,0,8" HorizontalAlignment="Center"/>
        <TextBlock Grid.Row="1" Text="Searching for your KICKR SNAP..." FontSize="18" Foreground="Gray" Margin="0,0,0,32" HorizontalAlignment="Center"/>
            
        <ListBox x:Name="ListDevices" Grid.Row="2" Margin="0,0,0,24" BorderThickness="1" BorderBrush="#EEEEEE" 
                 ScrollViewer.VerticalScrollBarVisibility="Auto" DisplayMemberPath="Name">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Padding" Value="12"/>
                    <Setter Property="FontSize" Value="16"/>
                </Style>
            </ListBox.ItemContainerStyle>
        </ListBox>

        <StackPanel Grid.Row="3">
            <TextBlock x:Name="TxtStatus" Text="Status: Initializing..." Margin="0,0,0,16" HorizontalAlignment="Center" Foreground="Gray"/>
            <Button x:Name="BtnConnect" Content="Connect and Continue" Click="BtnConnect_Click" Height="50" Width="300" FontSize="18" FontWeight="SemiBold" IsEnabled="False" Background="#0078D7" Foreground="White"/>
            <Button x:Name="BtnRescan" Content="Rescan" Click="BtnRescan_Click" Margin="0,12,0,0" Background="Transparent" BorderThickness="0" Foreground="#0078D7" Cursor="Hand"/>
        </StackPanel>
    </Grid>
</UserControl>